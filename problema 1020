#include <stdio.h>
 
int main() {
	
	int n;
	
	scanf("%d", &n);
	
	int ano, meses, dias, resto;
	ano = n/365;
	resto = n%365;
	meses = resto/30;
	dias = resto%30;
	 
	printf("%d ano(s)\n", ano);
	printf("%d mes(es)\n", meses);
	printf("%d dia(s)\n", dias);

    return 0;
}
