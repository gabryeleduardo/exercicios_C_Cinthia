#include <stdio.h>
 
int main() {
	
	int n;
	
	scanf("%d", &n);
	
	int horas, minutos, segundos, resto;
	horas = n/3600;
	resto = n%3600;
	minutos = resto/60;
	segundos = resto%60;
	 
	printf("%d:%d:%d\n", horas, minutos, segundos);

    return 0;
}
